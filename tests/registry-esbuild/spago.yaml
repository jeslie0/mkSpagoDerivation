package:
  dependencies:
    - console
    - effect
    - prelude
    - maybe: "*"
  name: registry-test
workspace:
  backend:
    cmd: "purs-backend-es"
    args:
    - "build"
  package_set:
    registry: 43.1.0
